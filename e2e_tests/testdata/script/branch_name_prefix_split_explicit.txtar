# Test that av branch --split with explicit name applies the configured prefix.
set-branch-prefix user/dev/

exec av branch base-branch
commit-file file1.txt content1
commit-file file2.txt content2

exec av branch --split my-explicit-branch

exec git branch --show-current
stdout '^user/dev/my-explicit-branch$'
